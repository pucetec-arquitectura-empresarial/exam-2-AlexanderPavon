package com.pucetec.exam2.routes

object Routes {
    const val BASE_URL = "/api/parking"

    const val AVAILABILITY = "$BASE_URL/availability"

    const val ENTRY = "$BASE_URL/entry"

    const val EXIT = "$BASE_URL/exit"
}